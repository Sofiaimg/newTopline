<p id="notice"><%= notice %></p>

<hr>
<table class="table table-striped table-bordered table-hover table-sm " id="myTable">
<thead>
  <tr>
    <th><strong>Quotation_detail:</strong></th>
    <th><strong>Quantity:</strong></th>
    <th><strong>Size:</strong></th>
    <th><strong>Ratio:</strong></th>

  </tr>
</thead>
<tbody>
    <tr>
      <td><%= @variant.quotation_detail.id %></td>
      <td><%= @variant.quantity %></td>
      <td><%= @variant.size %></td>
      <td><%= @variant.ratio %></td>

    </tr>
</tbody>

</table>
<%= link_to 'Edit', edit_variant_path(@variant) %> |
<%= link_to 'Back', variants_path %>
