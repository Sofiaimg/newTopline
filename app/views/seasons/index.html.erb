<p id="notice"><%= notice %></p>

<h1>Seasons</h1>

<div class="row">
  <div class="offset-md-3 col-md-6">
    <table class="table table-striped table-bordered table-hover table-sm text-center" id="myTable">
      <thead>
        <tr>
          <th>Season n°</th>
          <th>Name</th>
          <th>Status</th>
        </tr>
      </thead>
      <tbody>
          <% @seasons.each do |season| %>
            <tr>
              <td><%= season.id %></td>
              <td><%= season.name %></td>
              <td><%= season.closed? ? "Closed":"Open" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
